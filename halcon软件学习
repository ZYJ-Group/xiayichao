*读取图片
read_image (Image123, 'C:/Users/Lenovo/Desktop/0418.jpg')
*选择对应区域
rectangle1_domain (Image123, ImageReduced, 85, 290, 600, 760)
*灰度处理
rgb1_to_gray (ImageReduced, GrayImage)
*阈值操作，分割缺口
threshold(GrayImage,red,4,53)

*连通区域
connection (red, ConnectedRegions)
*选择圆环区域,基于区域面积（area）的筛选范围150 和 99999 ,单位为像素
select_shape(ConnectedRegions, SelectedRegions, 'area', 'and', 1500, 99999)
*将圆环区域中的面积剪出来
reduce_domain (GrayImage, SelectedRegions, ImageReduced1)

*将图像和高斯导数进行运算，4与高斯滤波器的标准差有关，用来控制平滑和导数计算的程度。
derivate_gauss (ImageReduced1, DerivGauss,2, 'gradient')
*分割区域轮廓
threshold (DerivGauss, Region, 8, 20)


area_center (ImageReduced1, Area, Row, Column)
gen_circle (Circle, Row, Column, 136)

union2 (Circle, red, RegionUnion)

*连通处理
connection (RegionUnion, ConnectedRegions1)
*补集
